name: cc_ui_kit
description: Shared widgets — scaffold primitives and reusable UI components.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  shadcn_flutter: ^0.0.52
  go_router: ^17.1.0
  cc_core:
    path: ../core

dev_dependencies:
  flutter_lints: ^6.0.0
