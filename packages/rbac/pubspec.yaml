name: cc_rbac
description: Application-layer authorization — Can widget + canProvider + seeder.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.6.1

  cc_core:
    path: ../core
  cc_fhir_models:
    path: ../fhir_models
  cc_data:
    path: ../data

dev_dependencies:
  flutter_lints: ^6.0.0
