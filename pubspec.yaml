name: clinical_curator_workspace
description: Workspace root — managed by melos. Apps live under apps/, shared code under packages/.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ^3.11.0

dev_dependencies:
  melos: ^6.3.2
